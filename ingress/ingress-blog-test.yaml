apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog-ingress
  namespace: default
spec:
  tls:
  - hosts:
    - my-blog.com
    secretName: my-tls-secret
  rules:
  - host: my-blog.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-lb-service
            port:
              number: 80
